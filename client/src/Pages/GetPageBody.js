import GetPageContent from './Components/GetPageContent';

export default function () {
    return (
        <div className="App-body">
            Method GET will be used here

            <GetPageContent />
        </div>
    );
}